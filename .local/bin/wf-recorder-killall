#!/usr/bin/env bash

set -o errexit
set -o nounset
set -o pipefail
if [[ "${TRACE-0}" == "1" ]]; then
    set -o xtrace
fi

#if ! (killall --signal SIGINT wf-recorder); then
if ! (killall wf-recorder); then
    notify-send "wf-recorder" "No recording started."
fi

